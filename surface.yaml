# Source of truth for the secondary public inspection index.
# Editing this file regenerates index.json and the README index table.
#
# Rules:
#   - Only the three current public inspection objects belong here.
#   - Each bounded_claim is one scope-qualified sentence.
#   - A pinned commit is evidence identity, not a claim that main cannot move.
#   - PATHS_RESOLVE does not mean the replay command was run.

version: 2
generated_at: "2026-08-30T05:58:10+00:00"
verification_receipt: receipts/verification-2026-08-30.json
repos:
  - name: start-here
    lifecycle_status: UNRELEASED
    evidence_status: PATHS_RESOLVE
    release_ref: null
    commit_sha: ebe5cd58ca3c4f87b13b1803e6281aa03027b0c9
    verified_at_utc: "2026-08-30T05:58:10+00:00"
    replay_status: NOT_RUN
    bounded_claim: >
      At this pinned commit, the declared demo asks whether state mutation can
      occur without a valid decision record on its demonstrated path.
    proof_path: run_demo.py
    test_command: "python run_demo.py"
    receipt_shape: RECEIPT.md

  - name: commit-gate-core
    lifecycle_status: UNRELEASED
    evidence_status: PATHS_RESOLVE
    release_ref: null
    commit_sha: a473af4a1fe3af81fe3c6442bdd75331f6a8126b
    verified_at_utc: "2026-08-30T05:42:48+00:00"
    replay_status: NOT_RUN
    bounded_claim: >
      At this pinned commit, the unreleased authorize-only kernel binds exact
      payload bytes and returns authorisation or refusal; it does not apply the
      payload.
    proof_path: src/commit_gate_core/authorize.py
    test_command: "PYTHONPATH=src python -m pytest tests/test_authorize.py tests/test_beau_failure_classes.py -q"
    receipt_shape: RECEIPT.md

  - name: obligation-bound-policy-admission-lab
    lifecycle_status: UNRELEASED
    evidence_status: PATHS_RESOLVE
    release_ref: null
    commit_sha: 0ac95d3439cf4ef79d2dc6873680c4be93cd0850
    verified_at_utc: "2026-08-30T05:42:48+00:00"
    replay_status: NOT_RUN
    bounded_claim: >
      At this pinned commit, the single-engine in-memory harness distinguishes
      historical admission, current standing and observed active state; it is
      not a production gate.
    proof_path: src/obpa_lab/
    test_command: "PYTHONPATH=src python -m unittest discover -s tests -v"
    receipt_shape: artifacts/release-manifest-v0.6.json
