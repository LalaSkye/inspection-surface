# Superseded System Map

The previous content collapsed separate repositories into a single mutation
architecture. That composition was not established by the indexed artefacts
and is withdrawn as a current public claim.

No current system topology is published here.

The active surface contains three separate inspection objects. See
[`index.md`](index.md). Evidence does not inherit between them.
